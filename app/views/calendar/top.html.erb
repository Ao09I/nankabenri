<h1>Calendar#top</h1>

<%= month_calendar events: @items do |date, items| %>
  <%= date %>

  <% items.each do |item| %>
    <div>
    <%= item.name %>
    <%= item.price %>
    <%= item.tweet_id %>


    </div>
  <% end %>
<% end %>