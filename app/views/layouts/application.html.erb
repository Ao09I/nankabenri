<!DOCTYPE html>
<html>
  <head>
    <title>Nankabenri</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <div class="header-logo">
        <%= link_to("なんかべんりなサイト", "/", class: "link") %>
      </div>
      <ul class="header-menus">
        <% if session[:user_id] %>
          <li> <%= @current_user.name %> </li>
          <li>
          <%= link_to("なんかべんりなサイトとは？", "/about", class: "link") %>
          </li>
          <li>
            <%= link_to("グッズ発売日カレンダー", "/calendar", class: "link") %>
          </li>
          <li>
            <%= link_to("交換掲示板", "/trade/index", class: "link") %>
          </li>
          <li>
            <%= link_to("ログアウト", "/logout", {method: "post"}) %>
        <% else %>
        <li>
          <%= link_to("なんかべんりなサイトとは？", "/about", class: "link") %>
        </li>
        <li>
          <%= link_to("グッズ発売日カレンダー", "/calendar", class: "link") %>
        </li>
        <li>
          <%= link_to("新規登録", "/signup", class: "link") %>
        </li>
        <li>
          <%= link_to("ログイン", "/login", class: "link") %>
        </li>
        <% end %>
        </ul>
    </header>

    <!-- <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %> -->
    <% flash.each do |message_type, message| %>
    <div class="flash">
      <%= message %>
    </div>
    <% end %>

    <%= yield %>
  </body>
</html>
